import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.smarthome',
  appName: 'com.example.smarthome',
  webDir: 'dist'
};

export default config;
